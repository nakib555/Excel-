import React from 'react';
import { Plus } from 'lucide-react';

const FocusCell = () => (
    <div className="flex items-center gap-1 text-[10px] text-green-700 font-medium cursor-pointer"><Plus size={10} /> Focus Cell</div>
);

export default FocusCell;